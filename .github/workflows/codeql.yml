# Caller stub -> reusable CodeQL workflow in poli0981/.github.
# Disable "Default setup" in repo Settings > Code security before the first run, or SARIF upload is rejected.
name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 4 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    uses: poli0981/.github/.github/workflows/reusable-codeql.yml@main
    with:
      languages: csharp
    secrets: inherit
